package utilities;

/**
 * The different cell states that a Node can take.
 */
public enum CellState {
    PLAYER,
    PATH,
    PATH_LOOKING,
    END,
    START,
    DEBUG,
    TARGET,
    VISITED,
    BACKGROUND
}
