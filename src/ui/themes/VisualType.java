package ui.themes;

/**
 * A helper class that dictates the type of variables needed for a theme.
 */
public enum VisualType {
  WALL,
  TARGET,
  BACKGROUND,
  VISITED,
  START,
  END,
  PATH,
  PLAYER,
  DANGER
}
